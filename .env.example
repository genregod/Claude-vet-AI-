# ── Valor Assist — Environment Variables ─────────────────────────────
# Copy this file to .env and fill in your keys.

# Anthropic API key (required for Claude 3.5 Sonnet)
ANTHROPIC_API_KEY=sk-ant-...

# Embedding provider: "huggingface" (free, local) or "voyageai" (legal-optimized)
EMBEDDING_PROVIDER=huggingface

# Voyage AI key (only needed if EMBEDDING_PROVIDER=voyageai)
VOYAGE_API_KEY=

# Claude model override (default: claude-3-5-sonnet-latest)
# CLAUDE_MODEL=claude-3-5-sonnet-latest

# Server
# HOST=0.0.0.0
# PORT=8000
