version: 1
backend:
  phases:
    build:
      commands:
        - '# Install Lambda function dependencies'
        - cd amplify/backend/function/chatbot/src && npm ci
        - cd amplify/backend/function/documentProcessor/src && npm ci
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - node -v
        - npm ci --prefix frontend
    build:
      commands:
        - echo "VITE_API_URL=$VITE_API_URL"
        - VITE_API_URL=${VITE_API_URL:-""} npm run build --prefix frontend
  artifacts:
    baseDirectory: frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
      - amplify/backend/function/chatbot/src/node_modules/**/*
      - amplify/backend/function/documentProcessor/src/node_modules/**/*
  customHeaders:
    - pattern: '**/*'
      headers:
        - key: X-Frame-Options
          value: DENY
        - key: X-Content-Type-Options
          value: nosniff
        - key: X-XSS-Protection
          value: '1; mode=block'
        - key: Strict-Transport-Security
          value: 'max-age=31536000; includeSubDomains'
        - key: Referrer-Policy
          value: strict-origin-when-cross-origin
        - key: Permissions-Policy
          value: 'camera=(), microphone=(), geolocation=()'
    - pattern: '/assets/*'
      headers:
        - key: Cache-Control
          value: 'public, max-age=31536000, immutable'
  buildSizeConfig:
    maxBuildSize: 200MB
customRules:
  - source: '</^[^.]+$|\.(?!(css|gif|ico|jpg|js|png|txt|svg|woff|woff2|ttf|map|json|webp)$)([^.]+$)/>'
    target: /index.html
    status: '200'
