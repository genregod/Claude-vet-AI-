# ── Valor Assist — AWS Deployment Configuration ─────────────────────
# Copy this file to scripts/aws_config.env and fill in your values.
# Used by: scripts/setup_aws_infrastructure.sh and scripts/deploy_aws.sh
#
# IMPORTANT: Never commit the filled-in aws_config.env to version control.

# ── AWS Account & Region ─────────────────────────────────────────────
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=

# ── ECR Repository Names ─────────────────────────────────────────────
ECR_REPOSITORY_BACKEND=valor-assist-backend
ECR_REPOSITORY_FRONTEND=valor-assist-frontend

# ── ECS Configuration ────────────────────────────────────────────────
ECS_CLUSTER_NAME=valor-assist-cluster
ECS_BACKEND_SERVICE=valor-assist-backend
ECS_FRONTEND_SERVICE=valor-assist-frontend
ECS_BACKEND_TASK_FAMILY=valor-assist-backend-task
ECS_FRONTEND_TASK_FAMILY=valor-assist-frontend-task

# Backend task resources
BACKEND_CPU=512
BACKEND_MEMORY=1024
BACKEND_DESIRED_COUNT=2

# Frontend task resources
FRONTEND_CPU=256
FRONTEND_MEMORY=512
FRONTEND_DESIRED_COUNT=2

# ── Networking ────────────────────────────────────────────────────────
VPC_CIDR=10.0.0.0/16
SUBNET_PUBLIC_1_CIDR=10.0.1.0/24
SUBNET_PUBLIC_2_CIDR=10.0.2.0/24
SUBNET_PRIVATE_1_CIDR=10.0.3.0/24
SUBNET_PRIVATE_2_CIDR=10.0.4.0/24

# ── Domain & SSL (optional) ──────────────────────────────────────────
# ACM certificate ARN for HTTPS on ALB
# ACM_CERTIFICATE_ARN=

# ── Application Secrets ──────────────────────────────────────────────
# These are stored in AWS Secrets Manager during setup
# ANTHROPIC_API_KEY=
# VOYAGE_API_KEY=
# ENCRYPTION_KEY=
# JWT_SECRET_KEY=

# ── Tags ──────────────────────────────────────────────────────────────
PROJECT_TAG=valor-assist
ENVIRONMENT_TAG=production
